---
interface Props {
  date: string;
}

const { date } = Astro.props;

const dateObject = new Date(date);
---

<time datetime={dateObject.toISOString()}>
  {
    dateObject.toLocaleDateString("cs", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
